{
  "name": "devcon-plus",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:member": "turbo run dev --filter=@devcon-plus/member",
    "dev:organizer": "turbo run dev --filter=organizer",
    "dev": "turbo run dev",
    "build": "turbo run build",
    "typecheck": "turbo run typecheck"
  },
  "devDependencies": {
    "turbo": "^2.3.3",
    "typescript": "^5.7.2"
  },
  "packageManager": "npm@11.9.0",
  "dependencies": {
    "framer-motion": "^12.34.3"
  }
}
